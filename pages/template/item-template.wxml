<template name="postItem">
  <view class="item-info">
    <text class="title"><text class="cate">{{cate}}</text><text class="fontline">|</text>{{title}}</text>
    <text class="time">{{time}}</text>
  </view>
  <image src="{{image}}" mode="widthFix" wx:if="{{image}}"></image>
</template>